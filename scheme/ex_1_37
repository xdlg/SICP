(define (cont-frac n d k)
    (define (cont-frac-with-counter i)
        (if (= i k)
            (/ (n i) (d i))
            (/ (n i) (+ (d i) (cont-frac-with-counter (+ i 1))))
        )
    )

    (cont-frac-with-counter 1)
)
